<div class="flexWrap">
    {{#each categories as |category|}}

    <div class="catIcons">
        <a href="/category/{{category.category_id}}">
            <img src="{{category.imagePath}}" alt="{{category.name}}" /></a>
    </div>

    {{/each}}
</div>
